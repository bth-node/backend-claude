PORT=3000
HOST=localhost

# MONGO_PORT controls the host-side port in docker-compose.yml
# MONGODB_URI must use the same port for the Node app to connect
MONGO_PORT=27017
MONGODB_URI=mongodb://localhost:27017/node

JWT_SECRET=change_this_to_something_long_and_secret
