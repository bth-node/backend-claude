meta {
  name: Get User by ID
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/users/{{userId}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.name: isString
  res.body.email: isString
}

