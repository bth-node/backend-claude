meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "{{testEmail}}",
    "password": "password123"
  }
}

assert {
  res.status: eq 200
  res.body.user.email: eq {{testEmail}}
}
