// Users routes — GET /api/users and GET /api/users/:id.
// See: issues/04-rest.md (in-memory) and issues/05-mongodb.md (database)

import { Router } from 'express'

const router = Router()

// TODO (rest): add in-memory users array and implement GET /api/users and GET /api/users/:id
// TODO (mongodb): replace the in-memory array with Mongoose queries

export default router
